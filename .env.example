# Vikunja MCP Server Configuration

# Your Vikunja instance URL (required)
VIKUNJA_URL=https://vikunja.example.com

# Your Vikunja API token (required)
# Get this from your Vikunja user settings
VIKUNJA_API_TOKEN=your-api-token-here

# Server mode (optional, defaults to stdio)
# Options: stdio, http
MCP_MODE=stdio

# HTTP server port (only used if MCP_MODE=http)
MCP_PORT=3000

# Enable debug logging (optional, defaults to false)
# When true, sets log level to DEBUG
DEBUG=false

# Specific log level (optional)
# Options: error, warn, info, debug
# If not set, defaults to 'info' (or 'debug' if DEBUG=true)
LOG_LEVEL=info

# Request timeout in milliseconds (optional)
REQUEST_TIMEOUT=30000

# Retry attempts for failed requests (optional)
RETRY_ATTEMPTS=3